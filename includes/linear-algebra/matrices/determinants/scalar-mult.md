Let $c$ be a scalar and $A$ be an $n$ by $n$ matrix.
Then $|cA| = c^n|A|$.

## Proof

$cA$ can be obtained from $A$ by applying the elementary row operation
$\langle i \rangle \leftarrow c\langle i \rangle$ on each row.

Therefore, $|cA| = c(c(\ldots(c(c|A|))\ldots)) = c^n|A|$.
